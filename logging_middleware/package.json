{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable Express logging middleware — Log(stack,level,package,message) posts to the test server",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "express": "^4.18.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
